// Define o pacote onde ficam as cores do tema do app
package com.example.logcatbutton.ui.theme

// Importa a classe Color do Jetpack Compose
// Ela serve para criar cores personalizadas usando hexadecimal
import androidx.compose.ui.graphics.Color

// COR PRINCIPAL DO APP (roxo padrão)
// Geralmente usada em botões, destaques e elementos principais
val PrimaryPurple = Color(0xFF7C3AED)

// ROXO MAIS ESCURO
// Usado para variações mais fortes do tema (ex: efeito pressionado, sombras)
val DarkPurple = Color(0xFF5B21B6)

// ROXO CLARO
// Usado em fundos suaves ou elementos secundários
val LightPurple = Color(0xFFC4B5FD)

// COR DE ERRO (VERMELHO)
// Usada para indicar ações negativas ou erros (ex: "Insuficiente")
val ErrorRed = Color(0xFFEF4444)

// COR DE ALERTA (LARANJA)
// Usada para situações intermediárias ou aviso (ex: "Regular")
val WarningOrange = Color(0xFFF59E0B)

// COR DE SUCESSO (VERDE)
// Usada para indicar algo positivo (ex: "Bom", aprovação)
val SuccessGreen = Color(0xFF22C55E)

// COR INFORMATIVA (ROXO/ROXO AZULADO)
// Usada para mensagens neutras ou informativas (ex: "Muito Bom")
val InfoPurple = Color(0xFF8B5CF6)

// COR DE FUNDO DO APP
// Usada como background geral da interface (tela inteira)
val BackgroundLight = Color(0xFFF5F3FF)
